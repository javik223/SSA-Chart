# Utils

Helper functions and utilities for the Claude Charts application.

## Modules

- `format.ts` - Number and date formatting utilities
- `colors.ts` - Color palettes and color utilities
- `math.ts` - Mathematical helpers (statistics, calculations)
- `export.ts` - Export utilities (PNG, SVG, JSON)
- `validation.ts` - General validation helpers
